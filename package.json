{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "npm run dev --prefix resources/admin",
        "build": "npm run build --prefix resources/admin",
        "build:vite": "cd resources/admin && npx vite build",
        "blocks:audit": "node scripts/audit-blocks.mjs"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^3.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0"
    },
    "dependencies": {
        "gsap": "^3.15.0"
    }
}
