{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "npm run dev --prefix resources/admin",
        "build": "npm run build --prefix resources/admin",
        "build:vite": "npx vite build --prefix resources/admin"
    },
    "devDependencies": {
        "concurrently": "^9.0.1"
    }
}
